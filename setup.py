from setuptools import setup

setup(name='src',
      version='0.0.1',
      description='Use ML to (re)construct MRI images more efficiently',
      url='https://github.com/act65/mri-reconstruction',
      packages=['src'],
     )
